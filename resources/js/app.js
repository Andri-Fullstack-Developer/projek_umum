import "./bootstrap";
import Swiper from "swiper";

document.addEventListener("DOMContentLoaded", function () {
    new Swiper(".swiper-container", {
        // tambahkan opsi-opsi swiper di sini
    });
});
